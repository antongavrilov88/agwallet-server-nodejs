describe('Auth API tests', () => {
    it('Should return 201 response status and token to signup request with valid data', (done) => {
        const x = true
        expect(x).toBe(true)
        done()
    })
})
