export enum UserRoutes {
    baseUserRoute = '/api/users',
    root = '/',
    userId = '/:id'
}

export enum AuthRoutes {
    baseAuthRoute = '/api/auth',
    signUp = '/signup',
    signIn = '/signin',
    signOut = '/signout'
}
